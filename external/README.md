# External dependencies (local)

This folder is for local clones of external repositories that we do not commit to this repo.

Current dependency:

- Tau Language: clone into `external/tau-lang`
  - `git clone https://github.com/IDNI/tau-lang external/tau-lang`
