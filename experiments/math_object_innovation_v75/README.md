## v75, width4 broad-scalar minimality frontier

### Structural target

Stress-test the `v74` three-scalar orbit law against a much wider scalar
support-plus-geometry library.

### Bounded domain

Abstract family:

- unlabeled `4`-subsets of the `4`-cube
- quotiented by cube automorphisms
- exhaustive orbit count:
  - `19`

Widened searched scalar library:

- support-count scalars
- support-sum scalars
- graph-degree scalars
- component scalars
- distance scalars
- per-distance count scalars
- parity and quadratic degree summary scalars

Total searched scalar features:

- `21`

### Strongest claim

On the full width-`4` orbit family:

- no searched singleton scalar is exact
- no searched scalar pair is exact
- exact scalar triples do exist
- one preferred exact triple remains:
  - `count_private_roles`
  - `max_degree`
  - `diameter`

So the `v74` three-scalar law was not an artifact of a too-small search
library.

Within this broader bounded search, the first exact scalar basis still has size
`3`.

### Claim tier

- `tier = descriptive_oracle`
- `oracle_dependent = true`
