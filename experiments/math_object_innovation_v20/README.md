# v20: score-block staircase frontier

## Structural target

Explain the next unsafe strata below the safe top block.

The bounded question is:

- after the `v19` score-safety collapse,
- do lower score blocks have simple anti-certificates,
- for example one shared first verifier refuter per block?

## Bounded domain

- the cached `7104` residual-consistent repair-program frontier from `v15`
- the cached `4263` unique verifier patterns from `v15`

## Allowed claim

If every member of a score block has the same first failing verifier pattern, that is a strong
block-level anti-certificate in this bounded model.
