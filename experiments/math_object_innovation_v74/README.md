## v74, width4 orbit scalarized mixed-law frontier

### Structural target

Replace the exact mixed basis from `v73`,

- one support-count coordinate
- plus one full distance multiset

with the smallest exact basis found inside the searched scalar
support-plus-geometry library.

### Bounded domain

Abstract family:

- unlabeled `4`-subsets of the `4`-cube
- quotiented by cube automorphisms
- exhaustive orbit count:
  - `19`

Searched scalar features:

- `count_private_roles`
- `count_size2_roles`
- `max_degree`
- `isolated_count`
- `diameter`

### Strongest claim

On the full width-`4` orbit family:

- no searched singleton scalar is exact
- no searched scalar pair is exact
- exact scalar triples do exist
- one preferred exact triple is:
  - `count_private_roles`
  - `max_degree`
  - `diameter`

So the first genuine width-`4` geometric obstruction is still small, but no
longer collapses to a scalar pair inside the searched scalar library.

### Claim tier

- `tier = descriptive_oracle`
- `oracle_dependent = true`
