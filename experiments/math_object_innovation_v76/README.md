## v76, width4 mixed-basis uniqueness frontier

### Structural target

Test whether the exact mixed-basis object from `v73` is one choice among many,
or whether the width-`4` orbit obstruction really singles out one geometry
summary.

### Bounded domain

Abstract family:

- unlabeled `4`-subsets of the `4`-cube
- quotiented by cube automorphisms
- exhaustive orbit count:
  - `19`

Widened tuple-aware mixed library:

- support-count coordinates
- full support profile
- degree sequence
- component sizes
- distance multiset

### Strongest claim

On the full width-`4` orbit family, within the searched tuple-aware mixed
library:

- the only nontrivial exact pair bases are:
  - `count_private_roles` plus `distance_multiset`
  - `count_size2_roles` plus `distance_multiset`

So the first width-`4` orbit obstruction is not just small.
It is also unusually rigid in the searched mixed library.

### Claim tier

- `tier = descriptive_oracle`
- `oracle_dependent = true`
